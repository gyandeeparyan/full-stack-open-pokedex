name: Hello World

on: [push]

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Say Hello
        run: echo "Hello World!"

      - name: Print the Date
        run: date

      - name: List Directory Content
        run: ls -la
